import javafx.scene.paint.Color;

public class Rectangle
{
   private double x;
   private double y;
   private Color color;
   
   public Rectangle(double x, double y, Color color)
   {
      this.x = x;
      this.y = y;
      this.color = color;
   }
}